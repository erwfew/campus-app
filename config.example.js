// 前端后端地址配置模板
// 使用方法：复制本文件为 config.js 并修改 BASE_URL 为你的后端地址
// config.js 已被 .gitignore 忽略，不会被提交
module.exports = {
  // 本地开发默认值；部署后改为如 https://your-server.onrender.com
  BASE_URL: 'http://localhost:3000'
}
